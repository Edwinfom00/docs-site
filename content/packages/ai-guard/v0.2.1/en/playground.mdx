---
title: Interactive Playground
description: Test all standalone modules of @edwinfom/ai-guard directly in your browser — no API key required.
---

# Interactive Playground

Run the standalone modules directly in your browser. All tests execute client-side — no API key, no network calls.

The tests below mirror the official smoke-test suite (`smoke-test.mts`) shipped with the package.

<SmokePlayground />
